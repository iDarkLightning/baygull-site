{
  "name": "baygull",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "dotenv -e .env -- pnpm -r --stream dev",
    "build": "dotenv -e .env -- pnpm -r --stream build",
    "wrangler:dev": "dotenv -e .env -- pnpm --filter @baygull/web wrangler:dev",
    "typecheck": "pnpm -r typecheck"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.8.0",
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "typescript": "^5.7.3"
  }
}
