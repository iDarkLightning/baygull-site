{
  "name": "baygull",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "dotenv -e .env -- pnpm -r --stream dev",
    "build": "dotenv -e .env -- pnpm -r --stream build",
    "wrangler:dev": "CLOUDFLARE_INCLUDE_PROCESS_ENV=true dotenv -e .env -- pnpm --filter @baygull/web wrangler:dev",
    "script:dev": "dotenv -e .env -- pnpm --filter @baygull/scripts execute",
    "script:prod": "dotenv -e .env.production -- pnpm --filter @baygull/scripts execute",
    "typecheck": "pnpm -r typecheck"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.8.0",
  "devDependencies": {
    "dotenv-cli": "^10.0.0",
    "typescript": "^5.7.3"
  }
}
